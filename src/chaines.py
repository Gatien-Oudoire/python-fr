class chaine:
    """
    Pour les chaines de caracteres
    """
    contenu : str
    taille : int = 0
    
    def __init__(self, o : int):
        if type(o) == int:
            o.

        elif type(o) == str:
            self.contenu = o

        for _ in self.contenu:
            self.taille += 1

            
            
